. ./.config


libraryname="${prefixtype}"

for i in "$@"
do	case "$i" in
		--libraryname=*)
			libraryname=${i#*=}
			;;
		*)
			fatalerror "$i"
			;;
	esac
done

mkdir -p "include/${libraryname}"

standardoutput "
prefix = $p
libraryname = $libraryname
" >> config.mak


